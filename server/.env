DATABASE_URL="postgresql://gatee@localhost:5432/project_reborn?schema=public"
JWT_SECRET="your_jwt_secret_here"
PORT=5001

# Comma-separated list of allowed frontend origins (for both HTTP + Socket.IO)
# Example: CORS_ORIGINS="https://your-app.vercel.app,https://your-custom-domain.com"
# Use "*" only for local testing.
CORS_ORIGINS="http://localhost:8080"
